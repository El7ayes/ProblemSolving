function triArea(base, height) {
    console.log(`when base of triangle is ${base} and height of triangle is ${height} The area of triangle ===>   ${(base * height) / 2}`);
    return `when base of triangle is ${base} and height of triangle is ${height} The area of triangle ===>   ${(base * height) / 2}`;
}
document.write(triArea(10, 5));