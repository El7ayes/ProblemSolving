function findPerimeter(length, width) {
    console.log(`The Perimeter of Rectangle is : ==> ${2 * (length + width)}`);
	return `The Perimeter of Rectangle is : ==> ${2 * (length + width)}`;
}
document.write(findPerimeter(2, 9));

// Perimeter of Rectangle law ==> 2 * (length + width)